# Contributing

Open an issue before a major scientific or architectural change. Add tests, provenance, documentation, and a limitation note with every new connector or model. Never commit secrets or restricted datasets. Run `make test`, `make lint`, `make typecheck`, and `python scripts/check_links.py` before a pull request.
