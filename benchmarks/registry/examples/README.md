# Registry examples

Examples in this directory are schema and workflow demonstrations only. They must not be cited as real-world scientific results.

To create local JSON templates:

```bash
heatsafe-registry templates --output artifacts/registry-templates
```
