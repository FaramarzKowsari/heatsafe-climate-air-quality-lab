# Model Card: Tiny LSTM Research Extension

A small CPU-compatible PyTorch class is provided as an optional neural baseline. It is not trained or ranked by default, and no superiority claim is made. Any future result must document windowing, normalization, seeds, hardware, early stopping, calibration, and comparison with persistence.
